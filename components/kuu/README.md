# components/kuu

「くぅ」の表示ロジックをここに閉じ込める。呼び出し側は `import { Kuu } from "@/components/kuu"` のみを使い、
内部実装(現在はSVG図形)を意識しない。

将来イラスト・AI生成画像・アニメーションへ差し替える際は、`Kuu.tsx` の内部実装のみを変更し、
呼び出し側のコードは変更しない(MVP仕様書§12.1・§14)。
